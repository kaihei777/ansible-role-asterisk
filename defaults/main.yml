---
# defaults file for ansible-role-asterisk
src_dir: /usr/src
jansson_version: "2.11"
asterisk_version: "13"

jansson_src_dir: "{{ src_dir }}/jansson-{{ jansson_version }}"
asterisk_src_dir: "{{ src_dir }}/asterisk"

epel_rpm_uri]: "https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm"
